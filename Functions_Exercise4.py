#Program to calculate sqrt using math module
#imporing math for sqrt

from math import sqrt

#Ask the user to provide a number to find the sqrt
number = int (input("Provide a number to find the sqrt: "))

print (sqrt(number))